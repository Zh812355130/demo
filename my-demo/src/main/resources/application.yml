server:
  port: 9100

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
  ai:
    dashscope:
      api-key: you key
      chat:
        options:
          model: qwen-max
    alibaba:
      a2a:
        server:
          version: 1.0.0
          card:
            name: data_analysis_agent
            description: 专门用于数据分析和统计计算的本地智能体
            provider:
              name: alibaba ai demo
              organization: spring ai h
              url: https://www.baidu.com
        nacos:
          discovery:
            enabled: false
          registry:
            enabled: false
